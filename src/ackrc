# only search with case sensitivity if there is mixed case
--smart-case
# follow symliunks
--follow

# treat these all as plaintext
--type-set
plaintext=.md,.markdown,.txt
# cucumber features
--type-set
cucumber=.feature
# treat PDFs as binary files
--type-set
bin=.pdf

# search haml, sass, erb, and coffeescript
--type-set=ruby=.haml,.rake,.erb,.rb,Gemfile
--type-set=css=.sass,.scss,.css
--type-set=js=.coffee,.js
--type-set=markdown=.markdown,.md,.mdown
--type-set=shell=.sh
--type-set=zsh=.zsh

# ignore binary files
--nobin

# Specify types to search
--ruby
--css
--js
--markdown
--html
--actionscript
--shell
--zsh

--ignore-dir=tmp
--ignore-dir=coverage
--ignore-dir=app/flash
--ignore-dir=cache

