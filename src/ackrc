# only search with case sensitivity if there is mixed case
--smart-case
# follow symliunks
--follow

# treat these all as plaintext
--type-set=plaintext=.md,.markdown,.txt
# cucumber features
--type-set=cucumber=.feature
# treat PDFs as binary files
--type-set=bin=.pdf

# search haml, sass, erb, and coffeescript
--type-add=ruby=.haml,Gemfile,.gemspec
--type-add=css=.sass,.scss
--type-add=js=.coffee
# and my custom types
--type-set=markdown=.markdown,.md,.mdown
--type-set=zsh=.zsh

# ignore binary files
--nobin

# Specify types to search
--ruby
--css
--js
--markdown
--html
--actionscript
--shell
--zsh

--ignore-dir=tmp
--ignore-dir=coverage
--ignore-dir=app/flash
--ignore-dir=cache
--ignore-dir=node_modules
--ignore-dir=public/assets
--ignore-dir=public/vite-dev
--ignore-dir=public/vite-test
--ignore-dir=vendor/assets
