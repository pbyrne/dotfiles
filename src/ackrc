# only search with case sensitivity if there is mixed case
--smart-case
# follow symliunks
--follow

# treat these all as plaintext
--type-set=plaintext=.md,.markdown,.txt
# cucumber features
--type-set=cucumber=.feature
# treat PDFs as binary files
--type-set=bin=.pdf
--type-set=js=.vue
--type-set=terraform=.tf

# search haml, sass, erb, and coffeescript
--type-add=ruby=.haml,Gemfile,.gemspec
--type-add=css=.sass,.scss
--type-add=js=.coffee
# and my custom types
--type-set=markdown=.markdown,.md,.mdown
--type-set=zsh=.zsh

# ignore binary files
--nobin
--nosvg

# Specify types to search
# --ruby
# --css
# --js
# --markdown
# --html
# --actionscript
# --shell
# --zsh
# --terraform

--ignore-dir=tmp
--ignore-dir=coverage
--ignore-dir=app/flash
--ignore-dir=cache
--ignore-dir=node_modules
--ignore-dir=public/assets
--ignore-dir=public/sitemap
--ignore-dir=public/vite-dev
--ignore-dir=public/vite-test
--ignore-dir=vendor/assets
--ignore-dir=log
--ignore-dir=.yarn
--ignore-dir=test/fixtures/vcr_cassettes
--ignore-dir=.docker-cache
--ignore-dir=.terraform
--ignore-dir=hypernova/dist
--ignore-dir=.cache
--ignore-dir=_site

--ignore-file=is:tags
