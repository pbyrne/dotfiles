# COMMENT OF SCRIPT HERE
# you can make as many tabs as you wish...
# tab names are actually arbitrary at this point too.

# server
tab do
  # run "be passenger start"
  # the bundled version of passenger isn't building correctly on lion
  run "passenger start"
end

# console
tab do
  run "be rails console"
end

# memcached
tab do
  run "be memcached -vv"
end
