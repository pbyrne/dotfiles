source :rubygems

gem 'rake'
# prolly don't need to include this; i want it on my machines, but it's not necessary for these scripts (the .terminitor files will get ignored if terminitor isn't installed)
# gem 'terminitor'